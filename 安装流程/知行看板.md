# 知行看板安装记录

## 2026-09-26：11.0.0 统一任务验收

- 平台与日期：Windows 11、PowerShell、Node.js 24；2026-09-26。正式服务已启用并登记 `accepted`，地址为 `http://127.0.0.1:47831/`。
- 源码：`C:\Users\6seve\.codex\worktrees\board-loop-engineering-20260926\Severin-skill\board`，候选提交 `1f9ee121779442f7c9189bb36be17af03acc7af7`；发布包为上层 `artifacts\project-board\severin-project-board-11.0.0.zip`，SHA-256 `8b5b3ab616b431a8b2d479902e088d862128621063025ee7e271a992a89f96b9`。
- 安装目录：`C:\Users\6seve\AppData\Local\SeverinBoardApp\11.0.0`；旧版 `C:\Users\6seve\AppData\Local\SeverinBoardApp\10.1.1` 保留。安装文件、待启用记录和源码包摘要均为 `0d55b05a96ba0fab23bd2d13738f72807a88e05e21a411ff46dad777591fef55`。
- 数据与配置：Windows 商店沙箱中的实际物理目录为 `C:\Users\6seve\AppData\Local\Packages\OpenAI.Codex_2p2nqsd0c76g0\LocalCache\Local\SeverinBoard`，包括 `local.json`、`events\`、`attachments\`、`personal-sessions\` 和 `pending-release.json`。`SEVERIN_BOARD_DATA` 由启动脚本指向该目录；没有新增 API Key。`/api/session` 的本机令牌只由正式页面临时使用，不写入安装记录。
- 自动启动：当前用户计划任务 `Severin Project Board`、启动文件夹快捷方式和开始菜单快捷方式由安装器登记。启动脚本为 `C:\Users\6seve\AppData\Local\SeverinBoardApp\11.0.0\scripts\start.ps1`。日常从开始菜单打开“Severin 项目看板”。
- 常用命令：在源码根目录运行 `pwsh -NoProfile -File board/scripts/package.ps1` 打包；`pwsh -NoProfile -File board/scripts/install.ps1` 安装；`Invoke-RestMethod http://127.0.0.1:47831/api/session` 核对真实版本；`Invoke-RestMethod http://127.0.0.1:47831/api/state` 核对任务状态。设置页“运行保障 → 启用统一任务验收”已开启；新看板任务与已连接项目的新 Codex 对话任务才进入新流程，旧任务保留原判定。
- 安装过程与踩坑：旧服务最初无响应，安装器只把新版置于独立目录，没有离线强切。首次自动接续未及时确认旧进程退出而放弃；重新恢复旧服务并由安装器取得唯一安装锁后完成切换。旧数据的逐文件核验备份为 `C:\Users\6seve\AppData\Local\SeverinBoardBackups\20260926-173349-50ff29d9`；较早的预备备份为 `C:\Users\6seve\AppData\Local\SeverinBoardBackups\20260926-172517-fd887545`。不能凭“包已复制”断言服务已经切换。
- 验证：正式 `/api/session` 为 11.0.0，旧任务 167、运行记录 335、人工保留 13、已停止 3；安装备份中的 13,210 个事件文件在正式目录缺失 0。Edge 正式页面 HTTP 200、脚本错误 0，统一验收开关刷新后仍开启；两类旧任务都显示 `legacy`。完整串行 Node 回归 589 通过、0 失败、1 条因未指定真实副本跳过，该副本专项随后 1/1 通过；设置页面专项通过。看板和 Codex 对话的写入与确认在隔离服务中核对，未在真实用户任务上提交测试反馈或伪造完成。
- 验收记录：源码 `board\acceptance.md` 的“11.0.0 统一任务验收正式安装”节；正式 `pending-release.json` 记录 `state=accepted`、摘要和离线备份路径。对话桥接另需已安装的知行插件 8.1.0，见 [severin-skill.md](severin-skill.md)。
- 回退：先停服务并核对正式数据新增事件，再以保留的 10.1.1 程序目录和上述离线备份作为对照恢复；不要直接把旧备份覆盖新增任务、聊天或事件。回退前先在副本核对新验收字段与旧版兼容性。
